# Database
DATABASE_URI=postgres://postgres:<password>@127.0.0.1:5432/your-database-name # Postgres DB
PAYLOAD_SECRET=YOUR_SECRET_HERE # Generate: openssl rand -base64 32
BLOB_READ_WRITE_TOKEN=YOUR_READ_WRITE_TOKEN_HERE # Vercel Blob Example

# WorkOS AuthKit Configuration
WORKOS_CLIENT_ID=client_xxx # Get from WorkOS Dashboard (dashboard.workos.com)
WORKOS_API_KEY=sk_xxx # Secret API Key from WorkOS Dashboard
WORKOS_COOKIE_PASSWORD=your-32-char-min-password # Generate: openssl rand -base64 32
NEXT_PUBLIC_WORKOS_REDIRECT_URI=http://localhost:3000/callback # Callback URL (use NEXT_PUBLIC for edge functions)

# Email Configuration (Optional - WorkOS handles auth emails)
RESEND_API_KEY=re_xxxxxxxx # For app-specific emails (not auth)
EMAIL_FROM=noreply@yourdomain.com # From address for app emails
